# Mismos valores que VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY en el
# .env.local de la app (src/lib/supabase.ts). La anon key no es secreta
# (esta protegida por RLS), pero igual: copia esto a .env y completalo a
# mano, no commitees el .env real.
SUPABASE_URL=
SUPABASE_ANON_KEY=
